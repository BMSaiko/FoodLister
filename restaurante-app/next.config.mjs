/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['example.com', 'images.secretlab.co'], // adicione outros domínios conforme necessário
    },
  };

export default nextConfig;
