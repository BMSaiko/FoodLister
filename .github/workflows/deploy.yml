name: Deploy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  deploy-preview:
    name: Deploy Preview
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Vercel CLI
        run: npm install -g vercel

      - name: Pull Vercel Environment
        run: vercel pull --yes --environment=preview --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      - name: Build Project
        run: vercel build --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy to Vercel Preview
        run: |
          DEPLOY_URL=$(vercel deploy --prebuilt --token=${{ secrets.VERCEL_TOKEN }})
          echo "PREVIEW_URL=$DEPLOY_URL" >> $GITHUB_ENV
          echo "Preview URL: $DEPLOY_URL"

      - name: Smoke Test Preview
        run: |
          echo "Running smoke test on ${{ env.PREVIEW_URL }}"
          # Wait for deployment to be ready
          sleep 15
          # Test API health endpoint (public, no auth required)
          API_STATUS=$(curl -s -o /dev/null -w "%{http_code}" "${{ env.PREVIEW_URL }}/api/health")
          if [ "$API_STATUS" != "200" ]; then
            echo "❌ Smoke test failed: /api/health returned $API_STATUS"
            exit 1
          fi
          # Test homepage returns a valid response (200, 302, or 401 are all acceptable)
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" "${{ env.PREVIEW_URL }}")
          if [ "$STATUS" -ge 500 ]; then
            echo "❌ Smoke test failed: homepage returned $STATUS (server error)"
            exit 1
          fi
          echo "✅ Smoke test passed! (health: $API_STATUS, homepage: $STATUS)"

  deploy-production:
    name: Deploy Production
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Vercel CLI
        run: npm install -g vercel

      - name: Pull Vercel Environment
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      - name: Build Project
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy to Vercel Production
        run: |
          DEPLOY_URL=$(vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }})
          echo "PROD_URL=$DEPLOY_URL" >> $GITHUB_ENV
          echo "Production URL: $DEPLOY_URL"

      - name: Smoke Test Production
        run: |
          echo "Running smoke test on production..."
          sleep 15
          # Test API health endpoint (public, no auth required)
          API_STATUS=$(curl -s -o /dev/null -w "%{http_code}" "${{ env.PROD_URL }}/api/health")
          if [ "$API_STATUS" != "200" ]; then
            echo "❌ Production smoke test failed: /api/health returned $API_STATUS"
            exit 1
          fi
          # Test homepage returns a valid response (200, 302, or 401 are all acceptable)
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" "${{ env.PROD_URL }}")
          if [ "$STATUS" -ge 500 ]; then
            echo "❌ Production smoke test failed: homepage returned $STATUS (server error)"
            exit 1
          fi
          echo "✅ Production smoke test passed! (health: $API_STATUS, homepage: $STATUS)"
